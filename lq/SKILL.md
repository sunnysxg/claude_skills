---
name: lq
description: >
  把当前这轮问答以 Q/A 形式追加记录到项目的 _sxg/qa_log.md。用户说 /lq 时使用。
---

把刚才这轮问答追加到当前项目的 `_sxg/qa_log.md`：

```markdown
### Q: <用户的问题>
A: <关于项目的事实回答>
```

规则：

- 只记录客观事实，不记录方案建议或实现思路
- 文件不存在则新建；若存在旧文件 `_sxg/问答记录.md`，先把它改名为 `qa_log.md` 再追加
- 追加，不覆盖
- 当前项目目录根据对话上下文判断（如 `factorhub/`）
