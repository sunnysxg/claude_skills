interface:
  display_name: "Session Log"
  short_description: "Archive and upsert the current task"
  default_prompt: "Use $session-log to archive this task with transcript-derived times and a searchable summary."
